-- create an Attendee table with a unique automatically incrementing primary key
-- called attendeeId with firstName, lastName, serviceId for PostgreSQL

-- create an Service table with a unique automatically incrementing primary key
-- called id with serviceId, desc for PostgreSQL

-- select serviceId from Attendee with inner join on Service table